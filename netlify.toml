[build]
  command = "npm run build"
  publish = "dist"

# Variables d'environnement de build à configurer dans Netlify :
# VITE_SUPABASE_URL
# VITE_SUPABASE_ANON_KEY
# VITE_VAPID_PUBLIC_KEY

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
